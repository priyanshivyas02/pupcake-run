import pygame
import sys
from pygame.locals import *

clock = pygame.time.Clock()
screen = pygame.display.set_mode((600,500))

bg = pygame.image.load("Pupcake Run Images/Pages/P1.png")


pygame.mouse.set_visible(0)
